@{
    ViewData["Title"] = "My Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>My Profile</h2>

<div>
   
    <div>
        @if (User.Identity.IsAuthenticated)
        {
            <div class="text-center my-5">
                <h1 class="display-4 fw-bold mb-4">@User.Identity.Name!</h1>
            </div>
        }
    </div>
</div>
